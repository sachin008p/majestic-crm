spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/majestic_crm
    username: postgres
    password: "5432"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    serialization:
      write-dates-as-timestamps: false

app:
  jwt-secret: majesticRealitiesSecretKey2024XYZabcdefghijklmnopqrstuvwxyz123456
  jwt-expiration-ms: 86400000
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:3004,http://localhost:3005,http://localhost:5173,http://localhost:5174"

server:
  port: 8080